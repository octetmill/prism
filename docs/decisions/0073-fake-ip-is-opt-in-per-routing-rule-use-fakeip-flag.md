# Fake-IP is opt-in per routing rule (`use_fakeip` flag)

Superseded — fake-IP is now a single global toggle (`dns.fakeip_enabled`) routing every proxied-domain query; the per-rule opt-in was removed. See *Routing rules reworked into a flat merged-destination model* and *Fake-IP destinations are TPROXY'd explicitly*.
