# Routing rules reworked into a flat merged-destination model; rule-sets fetched onto flash

Superseded — the flat-destination schema was replaced by child `condition` sections with AND/OR composition (see *Routing rules gain AND/OR condition composition*); rule-set delivery switched to sing-box-native `remote` rule-sets (see *Rule-sets switched from Prism-fetched local files to sing-box `remote` rule-sets*).
