# Manual Start/Stop/Restart buttons on Overview; UCI `enabled` flag is autostart-at-boot only

Superseded — `start_service` now gates on the UCI `enabled` flag itself, making it the single source of truth. See *Service start path rebuilt on homeproxy's model*.
