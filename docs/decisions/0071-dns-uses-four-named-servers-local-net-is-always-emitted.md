# DNS uses four named servers; local-net is always emitted

Superseded — DNS servers are now derived from routing rules, the hardcoded CN split is gone, and `local-net` was merged into a single `local` server. See *DNS server selection is derived from routing rules*.
