# Managed-DNS drop-in is written to dnsmasq's discovered per-instance conf-dir, not a hardcoded `/tmp/dnsmasq.d`

Superseded — conf-dir is now derived from the dnsmasq UCI section name; see *Managed-DNS conf-dir is derived from the dnsmasq UCI section name*.
